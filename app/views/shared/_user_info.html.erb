<%= link_to gravatar_for(current_user, size: Settings.avatar_small), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("profile"), current_user %></span>
<span><%= t "micropost", count: current_user.microposts.count %></span>
